這是一份基於您提供的新需求與先前的討論，專為初學者與軟體工程師打造的**現代化、簡潔、高 scannability 的 SPA（單頁式應用）軟體規格書**。

此版本強化了「多人連線限制（N個使用者）」**、**「Token 吞吐體感（流暢度）」**、以及**「四大情境適用度」的量化計算，讓初學者能一眼看懂自己主機的極限。

---

# 🚀 Ollama 本機與多人連線配置評估工具 — 現代化 SPA 規格書

## 1. 產品定位與一句話核心

> **「不只算容量，更能算載量。」**
> 這是一款視覺化的單頁 Web 工具，透過「模型權重 + 動態 KV Cache + 併發人數」的複合演算法，一鍵算出您的電腦能跑多大模型、能承載幾人同時連線、以及最真實的 Token 吞吐體感。

---

## 2. 核心功能規格

### 2.1 系統輸入與基本模式（極簡化）

初學者只需填寫以下 4 個核心欄位：

* **GPU VRAM (GB)：** 預設 `24`（如您的 RTX 4500/3090/4090）。
* **系統 RAM (GB)：** 預設 `32`。
* **主要用途：** 下拉選單（AI 聊天、軟體開發、OCR 文字辨識、語音轉文字）。
* **預期同時在線人數：** 數字輸入，預設 `1`。

### 2.2 進階模型與配置參數（含基礎說明提示）

當切換至「專家模式」時，提供以下參數調整，並附帶 **Hover Tooltip（懸浮說明）**：

| 參數名稱 | 預設值 | 界面控制 | 初學者基礎說明（Tooltip 內容） |
| --- | --- | --- | --- |
| **模型大小 (Params)** | `32B` | Slider / 下拉 | 模型參數量（十億）。通常越大越聰明，但也越吃顯存。 |
| **模型量化 (Quant)** | `Q4_K_M` | Radio 按鈕 | 模型壓縮率。**Q4 為黃金平衡**，檔案縮小 $1/4$ 但智商幾乎不減。 |
| **Context Window** | `32k` | Slider / 預設檔 | AI 的記憶長度。**軟體開發 Agent 強烈建議 64k 以上**。 |
| **KV Cache 精度** | `q8_0` | 下拉選單 | 儲存對話記憶的精度。**長對話建議設為 q8_0** 可省下一半顯存。 |

---

## 3. 畫面設計與現代化 UI 佈局 (Wireframe)

設計風格採用 **Tailwind CSS + Dark Mode（深色模式）**，搭配極具現代感的玻璃擬物化（Glassmorphism）卡片。

```
┌────────────────────────────────────────────────────────────────────────┐
│ 🚀 Ollama 本機與伺服器資源配置評估工具                                     │
├────────────────────────────────────────────────────────────────────────┤
│ 💻 電腦配置輸入                      │ 📊 匹配分數與體感報告 (Score: 88%) │
│ GPU VRAM: [ 24 ] GB                 │ ┌──────────────────────────────┐ │
│ 系統 RAM: [ 32 ] GB                 │ │🟢 完美匹配 (全 GPU 滿速運作)    │ │
│ 連線人數: [  2 ] 人                 │ └──────────────────────────────┘ │
│ 主要用途: [ 軟體開發 (Agentic) ˇ ]  │ 🔋 資源承載力極限：              │
├─────────────────────────────────────┤  • 模型載入極限：[ 1 個 32B 模型 ]│
│ 🧠 預期模型配置 (專家模式 [x])       │  • 併發人數上限：[ 2 人同時打字 ] │
│ 模型大小: (─○──────) Qwen-32B      │  • Context 上限：[ 每人最大 48k ] │
│ 量化等級: [ Q4 ]  Q5   Q8           │ ⚡ Token 吞吐體感：             │
│ 上下文長: (───○───) 64k Tokens     │  • [ 🏎️ 飛快 ] 預估 35~40 tokens/s│
│ KV Cache: f16  [ q8_0 ]  q4_0       │ 🎯 情境適用度：                 │
├─────────────────────────────────────┤  • 💬 Chat: ⭐⭐⭐⭐⭐             │
│ 🛠️ Ollama 環境變數一鍵複製            │  • 💻 Coding: ⭐⭐⭐⭐            │
│ [ PowerShell ]   [ Linux Bash ]     │  • 🔍 OCR / 🎙️ Audio: ⭐⭐⭐     │
└─────────────────────────────────────┴──────────────────────────────────┘

```

---

## 4. 關鍵計算邏輯與評估指標

### 4.1 四大資源極限算力評估

#### ① 只能載入 N 個多大的模型？

* **邏輯：** 為了確保不發生 CPU Offload 導致卡死，所有載入的模型總體積必須小於可用顯存。
* **公式：** $\text{最大模型載入數 (N)} = \text{floor}\left(\frac{\text{VRAM} - \text{系統預留 1.5G}}{\text{單個模型檔案大小}}\right)$
* **狀態輸出例：** 在 24G 顯存下，「只能載入 1 個 32B 模型」或「可同時載入 2 個 7B/14B 模型」。

#### ② 只能 N 個使用者同時使用？（併發限制）

* **邏輯：** 所有人共享同一個模型權重，但**每多一個人，就會獨立消耗一份 KV Cache 記憶體**。
* **公式：** 
$$\text{最大同時在線人數 (N)} = \text{floor}\left(\frac{\text{VRAM} - \text{模型檔案} - \text{Buffer}}{\text{單人 KV Cache 大小}}\right)$$


* **狀態輸出例：** 跑 32B 模型搭配 64k 上下文（單人吃 2.15GB），「極限為 2 個使用者同時提問」。

#### ③ 每個使用者 Context Window 上限？

* **邏輯：** 當輸入人數為 N 時，動態反算每個人最大可開的 Token 數。
* **公式：** $\text{單人最大 KV Cache 空間} = \frac{\text{VRAM} - \text{模型檔案} - \text{Buffer}}{\text{在線人數 } N}$，再由該空間反推 `contextTokens`。

#### ④ Token 吞吐體感（流暢度指標）

依據模型是否能 100% 留存在 VRAM 中，給出直觀的感官評價：

| 匹配分數 (Fit Ratio) | 速度狀態 | 介面顯示 | 體感說明文案 |
| --- | --- | --- | --- |
| **$< 0.85$** | 🏎️ 飛快 | 綠色 | 「字如泉湧 (35+ ts/s)」模型全在顯存，單人獨佔 GPU 算力。 |
| **$0.85 \sim 1.00$** | 🚗 流暢 | 黃色 | 「閱讀速度 (20~30 ts/s)」顯存接近飽和，多人同時打字時會微喘。 |
| **$1.00 \sim 1.15$** | 🛵 緩慢 | 橘色 | 「逐字蹦出 (5~10 ts/s)」部分模型溢出至系統 RAM，體驗變差。 |
| **$> 1.15$** | 🐌 極卡 | 紅色 | 「PPT 幻燈片 (<2 ts/s)」大量依賴系統 RAM 與 CPU，建議降級模型。 |

---

### 4.2 任務情境適用度評分機制（五星制）

結合「硬體資源」與「最新模型能力分布」，對四大情境進行智慧打分：

* **💬 一般聊天 (Chat)：** 最不吃 Context。只要模型能放進 VRAM 且參數大於 7B，即給予 **⭐⭐⭐⭐⭐**。
* **💻 軟體開發 (Coding)：** 極度看重 Context（建議 $\ge 64k$）。
* 若顯存能吃下 14B~32B 且 Context 開到 64k $\rightarrow$ **⭐⭐⭐⭐⭐**。
* 若顯存不足逼迫 Context 降到 8k $\rightarrow$ **⭐⭐**（因為 AI 馬上會忘記前文）。


* **🔍 OCR 文字辨識 / 🎙️ 語音轉文字 (Audio)：** * 這類任務主要依賴專門的視覺/語音多模態小模型（如 `minicpm-v`、`whisper`），不吃超大 Context，但需要快閃算力。若 VRAM 充足且優先速度 $\rightarrow$ **⭐⭐⭐⭐**。

---

## 5. 推薦決策演算法（前端核心自動優化）

當初使用者輸入「24G VRAM / 32G RAM / 軟體開發 / 2人連線」後，系統的自動降級與優化邏輯如下：

```顯示
[輸入配置] ──> 1. 自動鎖定「軟體開發」推薦的 64k Context
            ──> 2. 載入預設 32B Q4 模型 (20GB)
            ──> 3. 計算總需：20GB(模型) + 2.15GB×2人(KV Cache) = 24.3GB (超出顯存!)
            ──> 4. 【啟動自動優化機制】
                   ├── 優先步：將 KV Cache 精度由 f16 調降至 q8_0 (記憶體立刻減半)
                   └── 重新計算：20GB + 1.07GB×2人 = 22.14GB (符合 24G 限制)
            ──> 5. 輸出完美匹配報告，並產生對應的 OLLAMA_KV_CACHE_TYPE=q8_0 指令。

```

---

## 6. 技術棧與 MVP 驗收條件

* **前端架構：** Vue 3 + TypeScript + Vite（確保極速啟動）。
* **樣式與圖表：** Tailwind CSS（UI 介面）+ Floating UI（Tooltip 基礎說明）。
* **驗收條件 (AC)：**
* **AC-01：** 變更「連線人數」時，Context 上限與體感速度必須即時連動更新。
* **AC-02：** 四大情境（Chat/Coding/OCR/Audio）的星星評等必須精準反應硬體瓶頸。
* **AC-03：** 提供一鍵複製 PowerShell/Bash 環境變數的功能，包含 `OLLAMA_NUM_PARALLEL` 的正確人數限制。